configurations.configureEach {
  println "configureEach: $it"
  if (it.name == 'foo') {
    assert !it.canBeResolved
  }
}

configurations.register('foo') {
  canBeResolved = false
  println 'Register action'
}

// Realize the configuration
configurations.foo
